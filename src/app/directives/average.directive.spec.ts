import { AverageDirective } from './average.directive';

describe('AverageDirective', () => {
  it('should create an instance', () => {
    const directive = new AverageDirective();
    expect(directive).toBeTruthy();
  });
});
